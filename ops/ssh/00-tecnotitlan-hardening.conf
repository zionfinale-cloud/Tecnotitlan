# Tecnotitlan VPS: public-key access only.
# Loaded before cloud-init so its first-match values take precedence.
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin prohibit-password
